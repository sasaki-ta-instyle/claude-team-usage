# Database (Neon Postgres)
DATABASE_URL=postgres://user:pass@host/dbname?sslmode=require

# Auth.js v5
AUTH_SECRET=
AUTH_URL=http://localhost:3000/claude-team-usage
AUTH_TRUST_HOST=true

# Resend (magic link email)
AUTH_RESEND_KEY=
EMAIL_FROM=instyle group <noreply@instyle.group>

# Admin allowlist (comma-separated, lowercase emails). Must end with @instyle.group
ADMIN_EMAILS=sasaki-ta@instyle.group

# Anthropic Admin API
ANTHROPIC_ADMIN_API_KEY=sk-ant-admin01-...

# Sync endpoint bearer token (random secret used by cron)
SYNC_TOKEN=

# JPY conversion rate (used for display only)
JPY_PER_USD=155
